std_msgs/Header header
string vessel_name          # 船舶名称
string vessel_type          # 船舶类型 (如 "VIRTUAL_SIM")
float32 length              # 船长（米）
float32 width               # 船宽（米）
float32 speed_over_ground   # 对地航速（m/s）
float32 course_over_ground  # 对地航向（度，0-360，北偏角）