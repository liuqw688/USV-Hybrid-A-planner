# 参数作用及调节说明

本说明对应当前源码与配置，不承诺每个参数可在线更新。多数参数仅在构造时读取：编辑YAML后请停止旧实例并重新启动。launch内追加参数可能覆盖YAML；可用 `ros2 param dump /节点名` 核对运行值。

数值“增大/减小”指其他条件不变时的趋势，不是无限调大都有效。布尔值按开/关解释；话题和frame必须与生产者一致。不要同时降低安全半径、冗余及急停距离来追求直线效率。


## config/channel_navigation.yaml

当前配置值不是节点内置默认值，其他launch也可能覆盖它。

| 参数 | 当前YAML值 | 作用、关联及调大/调小影响 |
|---|---|---|
| `lane_data_topic` | `/chart_lane_data` | ROS消息接口名称（列表按目标对应）。修改需同时匹配发布方/订阅方及RViz；名称大小没有调参含义，失配会收不到数据并等待/停车。 |
| `costmap_topic` | `/local_costmap` | ROS消息接口名称（列表按目标对应）。修改需同时匹配发布方/订阅方及RViz；名称大小没有调参含义，失配会收不到数据并等待/停车。 |
| `odom_topic` | `/odom` | ROS消息接口名称（列表按目标对应）。修改需同时匹配发布方/订阅方及RViz；名称大小没有调参含义，失配会收不到数据并等待/停车。 |
| `goal_topic` | `/goal_pose` | ROS消息接口名称（列表按目标对应）。修改需同时匹配发布方/订阅方及RViz；名称大小没有调参含义，失配会收不到数据并等待/停车。 |
| `lane_costmap_topic` | `/channel/lane_costmap` | ROS消息接口名称（列表按目标对应）。修改需同时匹配发布方/订阅方及RViz；名称大小没有调参含义，失配会收不到数据并等待/停车。 |
| `marker_topic` | `/channel/navigation_markers` | ROS消息接口名称（列表按目标对应）。修改需同时匹配发布方/订阅方及RViz；名称大小没有调参含义，失配会收不到数据并等待/停车。 |
| `status_topic` | `/channel/status` | ROS消息接口名称（列表按目标对应）。修改需同时匹配发布方/订阅方及RViz；名称大小没有调参含义，失配会收不到数据并等待/停车。 |
| `outside_value` | `45` | 航道外类别编码。不是物理风险值；改变大小要同步全局/Hybrid/DWA类别约定。 |
| `opposite_value` | `75` | 左/对向航道类别编码。不是膨胀距离；改变编码要同步各规划器，否则分类失配。 |
| `main_lane_edge_value` | `20` | 主航道边沿软梯度上限。增大：边沿代价更高、更靠中心；减小：可贴边抄近路；须不超过各规划器主航道上界。 |
| `center_clearance` | `16.0` | 中心梯度的边界作用距离(m)。增大：更宽范围受到边沿排斥；减小：只有贴边才有代价；窄航道仍做中心处理。 |
| `minimum_course_speed` | `0.10` | 更新COG所需最小速度(m/s)。增大：低速时更多保持旧方向；减小：低速噪声更易翻转角色；不是最低实际船速。 |
| `reclassify_angle` | `0.2094395102` | COG策略变化触发语义重建的角度(rad)。增大：重建少、方向更新迟；减小：更灵敏但更吃资源且可能频繁触发全局纠偏。 |
| `lane_adjacency_distance` | `160.0` | 传播时航段多边形邻接距离(m)。增大：易连通断段、也易误连支流；减小：更严格、可能漏掉同向航段。 |
| `lane_continuity_angle` | `1.3962634016` | 邻接航段ORIENT最大变化(rad)。增大：接受急弯但更易串错航道；减小：同向急弯可能断开；不是全图与船头夹角。 |
| `reverse_enter_angle` | `1.9198621772` | 后方目标提前反向的方位差门槛(rad)。增大：更难触发翻转；减小：侧方目标也可能翻转，切目标时更易误分类。 |
| `reverse_complete_angle` | `0.6108652382` | COG接近反向任务方位后结束提前翻转的容差(rad)。增大：更早交回COG；减小：保持反向意图更久；需配合进入门槛。 |
| `minimum_reverse_goal_distance` | `10.0` | 允许提前反向的最小目标距离(m)。增大：近目标不会翻转；减小：近目标也可能反复改角色。 |

## 跨包一致性检查

- 当前本船最高速度4m/s；小型无人船等效规划半径均为0.5m、安全冗余1m，因此普通中心硬净空为2m。Marker长1m宽1.5m只是显示外形。
- Hybrid在7m软域提前规划、2m硬净空由Hybrid负责；tracking_only=true的DWA只跟踪Path，不使用目标船距离停车。
- AIS式远程监视保持80m/40s不变；行动层24m/16s、紧急层8m/6s。风险圈不是感知截止范围，也不等于软/硬避让域。
- 地图尺寸、原点、分辨率与frame应一致；地图膨胀和独立障碍层不得混淆。
- 主航道、外部、对向代价是偏好，安全硬过滤优先；降低偏好不解除危险栅格阻挡。
